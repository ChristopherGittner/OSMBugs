<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.gittner.osmbugs">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".App">
        <activity
            android:name="org.gittner.osmbugs.activities.OsmBugsActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.gittner.osmbugs.activities.BugEditorActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="org.gittner.osmbugs.activities.SettingsActivity" />
        <activity
            android:name="org.gittner.osmbugs.activities.AddMapdustBugActivity"
            android:label="@string/title_activity_add_mapdust_bug"></activity>
        <activity
            android:name=".activities.AddOpenstreetmapNoteActivity"
            android:label="@string/title_activity_add_openstreetmap_note"></activity>
    </application>

</manifest>
